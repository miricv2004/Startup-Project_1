/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.pexels.com','drive.google.com','res.cloudinary.com'],
    },
};


export default nextConfig;
